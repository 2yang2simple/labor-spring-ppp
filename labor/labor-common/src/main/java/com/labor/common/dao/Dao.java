package com.labor.common.dao;

/**
 * every dao are extends form here.
 * @author yang yang1
 *
 */
public interface Dao {
	
	/**
	 * dao can not be execute directly,
	 * it should be built from the daomanager
	 * and be executed in the service.
	 * @param dm
	 */

	
}
